# Architecture Sketch
Add a simple diagram (ASCII or an image) showing the relationship between your client, API layer, service layer, & DB.
* **Module Map:** List your key `src/` modules and their responsibilities.
